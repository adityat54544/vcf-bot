# Contributing to Aura VCF Bot

Thank you for your interest in contributing to Aura VCF Bot! We welcome contributions from everyone.

## 🚀 Getting Started

1. **Fork the repository** on GitHub
2. **Clone your fork** locally:
   ```bash
   git clone https://github.com/your-username/aura-vcf-bot.git
   cd aura-vcf-bot
   ```

3. **Set up the development environment**:
   ```bash
   python -m venv venv
   source venv/bin/activate  # On Windows: venv\Scripts\activate
   pip install -r requirements.txt
   ```

4. **Create a `.env` file** from the example:
   ```bash
   cp .env.example .env
   ```

## 📝 Contribution Guidelines

### Code Style
- Follow PEP 8 Python style guide
- Use type hints where appropriate
- Write clear, descriptive commit messages
- Keep functions and methods focused and concise

### Pull Requests
1. Create a new branch for your feature/fix:
   ```bash
   git checkout -b feature/your-feature-name
   ```

2. Make your changes and commit them:
   ```bash
   git add .
   git commit -m "Add descriptive commit message"
   ```

3. Push to your fork:
   ```bash
   git push origin feature/your-feature-name
   ```

4. Open a Pull Request to the main repository

### Issue Reporting
- Use the provided issue template
- Include clear reproduction steps
- Provide relevant logs and error messages
- Specify environment details (Python version, OS, etc.)

## 🧪 Testing

- Test your changes thoroughly before submitting
- Ensure existing functionality isn't broken
- Add tests for new features if possible

## 🤝 Code of Conduct

Please follow our [Code of Conduct](CODE_OF_CONDUCT.md) in all interactions.

## 🎉 Recognition

All contributors will be recognized in the project's CONTRIBUTORS.md file.

Thank you for contributing to Aura VCF Bot! 🚀
